# 1.2 npm 패키지 추가하기

{% hint style="info" %}
프로젝트 폴더 최상단 위치에서 아래 명령을 실행해주세요.&#x20;

```
yarn add adwhale-sdk-react-native
  또는
npm install adwhale-sdk-react-native
```



아래 링크에서도 추가 방법을 확인할 수 있습니다.

[https://www.npmjs.com/package/adwhale-sdk-react-native](https://www.npmjs.com/package/adwhale-sdk-react-native)
{% endhint %}

명령어를 실행하고 나면 package.json에서 다음과 같이 패키지가 추가된 것을 확인할 수 있습니다.

```json
  "dependencies": {
    "adwhale-sdk-react-native": "^2.7.202"
  },
```

TS(혹은 JS) 파일 Import

```typescript
import {
  AdWhaleMediationSdk,
  AdWhaleBannerView,
  AdWhaleInterstitialAd,
  AdWhaleInterstitialErrorEvent,
  AdWhaleRewardAd,
  AdWhaleRewardErrorEvent,
  AdWhaleRewardUserRewardedEvent,
  AdWhaleAppOpenAd,
  AdWhaleAppOpenErrorEvent,
  AdWhaleNativeTemplateView,
  AdWhaleNativeTemplateHandle,
  AdWhaleNativeTemplateStyle,
  AdWhaleNativeTemplateType,
  AdWhaleNativeCustomView,
  AdWhaleNativeCustomHandle,
  AdWhaleNativeTemplateError,
  AdWhaleNativeCustomError,
} from 'adwhale-sdk-react-native';
```

### 최신 SDK version : 2.7.202

### Version Policy

본 SDK는 Android 네이티브 SDK 버전과의 대응 관계를 명확히 유지하기 위해 다음과 같은 버전 규칙을 사용합니다.

* 앞 세 자릿수(Major.Minor.Patch) 는 Android SDK 버전과 동일
* React Native 전용 수정 사항은 Patch 값을 확장하여 관리

#### Version Mapping Example

<table><thead><tr><th width="202.140625">Android SDK</th><th width="198.82421875">React Native SDK</th><th>Description</th></tr></thead><tbody><tr><td>2.7.1</td><td>2.7.100</td><td>Android 2.7.1 기반 RN SDK 최초 릴리스</td></tr><tr><td>2.7.1</td><td>2.7.101</td><td>RN 레이어 핫픽스</td></tr><tr><td>2.7.1</td><td>2.7.102</td><td>RN 버그 수정</td></tr><tr><td>2.7.2</td><td>2.7.200</td><td>Android SDK 2.7.2 반영</td></tr></tbody></table>

### Version Usage Guide

프로젝트 상황에 따라 아래 방식을 권장합니다.

#### Android SDK 버전에 고정하여 사용하는 경우 (권장)

운영 안정성을 중시하는 경우

```
{
  "adwhale-sdk-react-native": "~2.7.202"
}
```

* Android SDK 2.7.2 기반 유지
* RN 핫픽스만 자동 반영

#### 동일한 Android SDK 기반의 최신 RN 수정만 사용

```
{
  "adwhale-sdk-react-native": "^2.7.202"
}
```

* Android SDK 2.7.x 범위 내 최신 RN 수정 자동 반영
* Android SDK 상위 메이저/마이너 버전은 반영되지 않음

#### 특정 버전을 완전히 고정하여 사용하는 경우

```
{
  "adwhale-sdk-react-native": "2.7.202"
}
```

#### 항상 최신 버전을 사용하는 경우

```
{
  "adwhale-sdk-react-native": "latest"
}
```

* 최신 Android SDK 및 RN SDK 포함
* 개발 및 검증 환경에 적합
